apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: prd-project
  namespace: argocd
spec:
  clusterResourceWhitelist:
    - group: "*"
      kind: "*"
  destinations:
    - name: in-cluster
      namespace: production
  sourceRepos:
    - '*'

